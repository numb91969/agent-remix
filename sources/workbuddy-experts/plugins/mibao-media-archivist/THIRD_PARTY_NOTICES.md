# 秘宝26.9.6第三方与来源说明

本专家包包含MIBAO第一方Python源码、政策、schema、文档、模板和沿用26.8.29的头像。第一方内容依随包LICENSE管理。来源基线与具体复用映射由开发仓库evidence/source-baseline.json记录；历史候选的验证或上架状态不被继承。

包内不含第三方可执行文件、DLL、wheel、虚拟环境、模型权重、字体或测试媒体。Python和SQLite/FTS5由宿主提供，其许可证不由本包替代。

FFmpeg/ffprobe、Pillow、faster-whisper、sentence-transformers和Tesseract仅是外部可选能力。可选安装的依赖及模型需要使用者在指定环境中核对各自许可证与来源；相关代码/二进制/权重未随本ZIP分发。核心使用不要求安装它们。

用户照片、视频、录音、字幕、路径映射及测试产物均不进入提交ZIP。头像按原上架版本保持不变。
