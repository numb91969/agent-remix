# AI review policy

Low-confidence OCR, ASR, face, person, location, and scene results enter a review queue. The original media and raw AI output are retained. A human-confirmed value is stored as a separate field with reviewer and review time. Never overwrite raw model output with a human edit.
