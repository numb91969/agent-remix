# Mibao local model policy

- Models are managed by Mibao, not selected through arbitrary user shell commands.
- Every model has a pinned version, source, checksum, license, and commercial-use note.
- Original media stays local by default.
- If a model runtime is unavailable, the task is blocked and reported; no synthetic result is returned.
- AI results are derived data, never replacements for originals.
