# Privacy boundary

The expert uses the current task, user-supplied public material and public web capability exposed by the host. It does not require a connector, private CRM, email, calendar, hidden session history or credentials. External writes and contact remain disabled. If the host exposes optional storage or search, the user and host permission boundary controls that action; package presence alone is not permission.
