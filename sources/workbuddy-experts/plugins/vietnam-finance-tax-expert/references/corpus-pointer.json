{
  "name": "vietnam-finance-tax-corpus",
  "version": "2.0.0",
  "description": "Vietnam finance, tax, banking, forex, compliance and legal corpus for the Vietnam Finance & Tax Expert agent. 701 files (333 HTML + 323 TXT + 43 PDF + 1 MANIFEST.json + 1 PDF). Permanently embedded in expert package, no deployment/unzip needed.",
  "fileCount": 701,
  "manifestFile": "MANIFEST.json",
  "corpusPath": "~/.workbuddy/plugins/marketplaces/my-experts/plugins/vietnam-finance-tax-expert/references/corpus/vietnam-finance-tax-corpus/",
  "accessMethod": {
    "type": "permanent-embedded",
    "description": "Corpus files are permanently unzipped inside the expert package. Read directly via Read/Grep/Glob tools — no unzip, no deployment, no download, no network required.",
    "pathResolution": "~ auto-resolves to the user's home directory on any OS (Windows: C:\\Users\\{user}\\, macOS/Linux: <local-user-path>). The corpus is always at the same relative path inside the expert package."
  },
  "selfContainmentStatement": "100% self-contained and permanently ready. No zip, no API keys, no cloud credentials, no S3/COS bucket access, no network connection, no deployment step required. Corpus files are physically inside the expert package and travel with it to any computer.",
  "integrity": {
    "md5Verified": true,
    "verifiedDate": "2026-07-21",
    "portabilityTestPassed": true,
    "portabilityTestDetails": {
      "zeroCredentialsFound": true,
      "zeroCloudStorageURLs": true,
      "zeroEnvOrConfigFiles": true,
      "zeroZipFilesNeeded": true,
      "cdmUrlsInHTML": "decorative only (images/CSS), all HTML files have corresponding .txt versions that are fully self-contained",
      "crossMachineTest": "passed - corpus permanently embedded, no deployment step, files directly readable on any machine where expert package is installed"
    }
  },
  "knownHtmlCdnNote": "Some HTML files contain CDN image/CSS URLs (e.g., luatvietnam.s3.ap-southeast-1.amazonaws.com) from original web sources. These are decorative-only and do not affect text content readability. Every HTML file with CDN references has a corresponding .txt version that is fully self-contained with zero external dependencies. The string 'AKIArAC2AMAAygDUAN4A' found in some HTML is a base64 font encoding fragment, NOT a real AWS access key."
}
