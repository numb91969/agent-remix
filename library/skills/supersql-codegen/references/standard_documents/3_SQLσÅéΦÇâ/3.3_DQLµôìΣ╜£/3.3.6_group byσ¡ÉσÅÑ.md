# group by子句

## 说明

支持group by，支持一条语句中包含多个聚集函数，如：

```sql
SELECT pv_users.gender, COUNT(DISTINCT pv_users.userid), COUNT(1), SUM(DISTINCT pv_users.userid)
FROM pv_users
GROUP BY pv_users.gender;
```

## 注意事项

### 多DISTINCT限制

目前不支持一条语句含有多个对不同列的dinstinct聚集函数。

例如下面的语句暂时不支持：

```sql
INSERT OVERWRITE TABLE pv_gender_agg
SELECT pv_users.gender, COUNT(DISTINCT pv_users.userid), COUNT(DISTINCT pv_users.ip)
FROM pv_users
GROUP BY pv_users.gender;
```

这个功能在下一个版本将支持。

### 多GROUP BY优化

支持一条语句多group by，对于这样的语句TDW将做优化，它的运行效率将比两条高。

```sql
FROM pv_users
INSERT OVERWRITE TABLE pv_gender_sum
  SELECT pv_users.gender, COUNT(DISTINCT pv_users.userid)
  GROUP BY pv_users.gender
INSERT OVERWRITE DIRECTORY '/user/facebook/tmp/pv_age_sum'
  SELECT pv_users.age, COUNT(DISTINCT pv_users.userid)
  GROUP BY pv_users.age;
```

## 常见问题

### ORDER BY报错码问题

如果order by报错码：`Invalid Table Alias or Column Reference xx`（实际列存在）

**解决办法**：order by 的时候使用别名
