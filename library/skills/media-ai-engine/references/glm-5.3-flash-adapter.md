# 多模态宿主适配合同

本路径作为旧版引用的兼容锚点保留。26.9.6不指定固定模型或把厂商能力当作WorkBuddy专家入口已验证能力。

以当前host/model/entry/permission和测试回执确定实际可读媒体；不可用时优先既有字幕、已验证本地转写或有界证据帧。模型job需要实际执行和结果回填，不能在生成任务后宣称分析完成。

模型切换只使对应能力回执失效。旧派生和用户确认继续保留；图像修复或生成内容必须独立标为派生作品。
