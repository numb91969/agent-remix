# 展示模板：流程列表（workflow-list）

## 用途
渲染 `ListWorkflow` 接口返回的流程列表数据（场景一/场景三的 Markdown 表格输出）。

## 输出格式

```markdown
## {场景标题}（共 {Total} 条）

| 流程名称 | 流程ID | 分类 | 状态 | 更新时间 | 发起量 |
|---------|--------|------|------|---------|--------|
| {Name} | {Id} | {Category} | {Status_Emoji} | {UpdateTime} | {InstanceCount}次 |

> 输入流程 ID（P 开头）可查看画布详情。
```

### 场景标题规则

| ListType | 标题 |
|----------|------|
| `Admin` | 我的流程 |
| 不传 / `FlowListTypeDefault` + `IsPublic:"true"` | 公开流程 |

## 状态 Emoji 映射（流程模板状态，非流程单状态）

用户在流程列表里可见的状态只有 **3 种**：已启用、已停用、未启用。其中「未启用」由后端的 `Ready`（就绪）与 `Draft`（草稿）两个原始值**合并**而来，展示时统一显示为「未启用」，不再对用户区分就绪/草稿。

| Status 原始值 | 归并展示 |
|--------------|---------|
| `Enable` | ✅ 已启用 |
| `Disable` | ⛔ 已停用 |
| `Ready` / `Draft` | ⚪ 未启用 |

## 空结果处理

```markdown
暂无符合条件的流程。

建议：
- 检查关键字拼写是否正确
- 确认当前账号是否有流程查看权限
- 尝试不带关键字查询全部流程
- 切换查询范围（我的流程 ↔ 公开流程）试试
```

## 分页提示（有更多时追加）

```markdown
> 已展示 {显示数} 条，共 {Total} 条。如需查看更多，请告诉我「下一页」或「查看全部」。
```

## 示例输出

```markdown
## 我的流程（共 14 条）

| 流程名称 | 流程ID | 分类 | 状态 | 更新时间 | 发起量 |
|---------|--------|------|------|---------|--------|
| 商品 SKU 上架审批 | P2026051400000274 | 产商品库 | ✅ 已启用 | 2026-05-19 | 628次 |
| 计费规则变更申请 | P2026051200000102 | 计费平台 | ✅ 已启用 | 2026-05-18 | 412次 |
| 官网内容发布 | P2026050900000089 | 官网平台 | ✅ 已启用 | 2026-05-15 | 851次 |
| 商品下架审批 V2 | P2026051500000301 | 产商品库 | ⚪ 未启用 | 2026-05-19 | — |

> 输入流程 ID（P 开头）可查看画布详情。

---

💡 **还可以帮你做更多**：
- 📋 **更全/筛选**：需要查看全量流程列表吗？或按状态、分类等条件筛选？
- 📊 **格式化输出**：需要我将流程信息整理成 Excel 表格吗？
- 📈 **统计分析**：需要我统计流程发起情况，并生成统计报告吗？

> 以上数据均来自流程平台（https://huijin.woa.com/flow）
```
